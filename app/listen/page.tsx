export default function ListenPage() {
  return (
    <main className="page-shell">
      <section className="page-hero">
        <span className="eyebrow">Migration placeholder</span>
        <h1>Listen</h1>
        <p>
          This route preserves `/listen/` and will eventually host the audio layer, episode
          framing, and current listening links.
        </p>
      </section>
    </main>
  );
}
