export default function TheStoryPage() {
  return (
    <main className="page-shell">
      <section className="page-hero">
        <span className="eyebrow">Migration placeholder</span>
        <h1>The Story</h1>
        <p>
          This route preserves `/the-story/` and will hold the narrative framing, format logic,
          and story architecture migrated from WordPress.
        </p>
      </section>
    </main>
  );
}
