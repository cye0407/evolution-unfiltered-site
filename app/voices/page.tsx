export default function VoicesPage() {
  return (
    <main className="page-shell">
      <section className="page-hero">
        <span className="eyebrow">Migration placeholder</span>
        <h1>Voices</h1>
        <p>
          This route preserves `/voices/` and will become the structured character and POV layer
          for the site.
        </p>
      </section>
    </main>
  );
}
